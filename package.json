{
  "name": "web-comboilabs",
  "version": "1.0.0",
  "private": true,
  "description": "Web corporativa de Comboi Labs (HTML estatico + funciones serverless de Vercel).",
  "scripts": {
    "migrate": "node scripts/migrate.mjs",
    "hash-password": "node scripts/hash-password.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "bcryptjs": "^2.4.3"
  }
}
